apiVersion: v1
kind: ConfigMap
metadata:
  name: skillcrm-config
  namespace: twentycrm
data:
  # Application settings
  NODE_ENV: "production"
  NODE_PORT: "3000"
  
  # Email settings
  EMAIL_FROM_ADDRESS: "noreply@skillcrm.ru"
  EMAIL_FROM_NAME: "SkillCRM"
  EMAIL_SYSTEM_ADDRESS: "system@skillcrm.ru"
  EMAIL_DRIVER: "smtp"
  
  # Logging
  LOG_LEVEL: "warn"
